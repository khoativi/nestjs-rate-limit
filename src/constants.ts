/**
 * Module options token for rate limiting.
 */
export const RATE_LIMIT_MODULE_OPTIONS = 'RATE_LIMIT_MODULE_OPTIONS';

/**
 * Constant key for storing rate limit options metadata.
 */
export const RATE_LIMIT_OPTIONS = 'RATE_LIMIT_OPTIONS';

/**
 * Key used to indicate that a route should skip rate limiting.
 */
export const SKIP_RATE_LIMIT = 'SKIP_RATE_LIMIT';
